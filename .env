# Pinterest API Credentials
PINTEREST_ACCESS_TOKEN=<your_access_token_here>
PINTEREST_CLIENT_ID=<your_client_id_here>
PINTEREST_ACCOUNT_ID=<your_ad_account_id_here>

# Data Collection Configuration
DATA_FETCH_INTERVAL=30             # Days to look back for fetching data (e.g., 30 days)
API_REQUEST_LIMIT=500              # Limit for API calls per request batch (set by Pinterest or adjust as needed)
RETRY_ATTEMPTS=3                   # Number of retries for failed API calls
REQUEST_TIMEOUT=10                 # Timeout (in seconds) for each API request

# Model Training Parameters
TRAIN_TEST_SPLIT_RATIO=0.8         # Split ratio for train-test data (0.8 for 80% train, 20% test)
RANDOM_SEED=42                     # Random seed for reproducibility in data splits and model training
MODEL_SAVE_PATH=./models/ctr_model.pkl  # Path where the trained model will be saved

# Optional Database Configuration (if using for storing fetched data or results)
# DATABASE_URL=your_database_url    # URL for a database if you plan to use one (e.g., SQLite, PostgreSQL)
# DATABASE_NAME=ctr_database        # Database name for structured storage
